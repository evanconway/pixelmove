smooth_move = new SmoothMove(x, y);
smooth_move_show_stairsteps(smooth_move, false);

smooth_move_set_position(smooth_move, room_width/2, room_height / 5);

trail = new PositionTrail(250, 1/250);

angle = 0;
