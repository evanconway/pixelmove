pixel_move = new PixelMove(x, y);
pixel_move_set_movement_type_smooth(pixel_move);
pixel_move_set_position(pixel_move, room_width/2, room_height / 5);

trail = new PositionTrail(250, 1/250);

angle = 0;
